# Scraper для АЙМАҚ АҚШАМЫ

Этот скрипт скачивает последние статьи с сайта https://aimaqaqshamy.kz/ вместе со всеми изображениями и метаданными.

## Установка

1. Убедитесь, что у вас установлен Python 3.7+
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

## Использование

### Базовое использование

Скачать последние 30 статей:

```bash
python scrape_aimaq.py
```

### Результаты

После выполнения скрипта будет создана папка `scraped_data/` со следующей структурой:

```
scraped_data/
├── articles.json       # JSON файл со всеми статьями и метаданными
└── images/            # Папка со всеми скачанными изображениями
    ├── article-slug_image1.jpeg
    ├── article-slug_image2.png
    └── ...
```

## Структура данных

Каждая статья в `articles.json` содержит:

```json
{
  "url": "https://aimaqaqshamy.kz/article-url/",
  "title": "Заголовок статьи",
  "date_published": "2025-12-02T04:57:53+00:00",
  "date_modified": "2025-12-02T04:57:53+00:00",
  "author": "admin",
  "content": "Полный текст статьи...",
  "thumbnail_url": "https://...",
  "images": [
    {
      "url": "https://aimaqaqshamy.kz/wp-content/uploads/...",
      "local_path": "scraped_data/images/article-slug_image.jpeg",
      "alt": "Alt текст",
      "width": "1125",
      "height": "639",
      "is_thumbnail": true
    }
  ],
  "scraped_at": "2025-12-03T10:30:00"
}
```

## Настройка

Вы можете изменить параметры в файле `scrape_aimaq.py`:

```python
scraper = AimaqScraper(
    base_url="https://aimaqaqshamy.kz",  # URL сайта
    output_dir="scraped_data"             # Папка для сохранения
)
scraper.scrape_articles(num_articles=30)  # Количество статей
```

## Импорт в базу данных

После скрапинга вы можете использовать `articles.json` для импорта данных в вашу базу данных. Файл содержит:

- ✅ Заголовки статей
- ✅ Даты публикации (ISO 8601 формат)
- ✅ Полный контент статей
- ✅ Все изображения (скачаны локально)
- ✅ Автор
- ✅ Оригинальные URL

## Особенности

- 🔄 Автоматическое скачивание всех изображений
- 📅 Извлечение дат в стандартном формате
- 🛡️ Защита от дублирования (не скачивает одно изображение дважды)
- ⏱️ Задержки между запросами для вежливого отношения к серверу
- 📝 Детальное логирование процесса

## Требования к системе

- Python 3.7+
- Интернет соединение
- ~100MB свободного места (для 30 статей с изображениями)

## Устранение проблем

### Ошибка "Connection timeout"
Проверьте интернет соединение. Скрипт автоматически установит таймаут 30 секунд.

### Не все изображения скачались
Некоторые изображения могут быть недоступны или URL могут быть невалидными. Проверьте логи.

### JSON не читается правильно
Убедитесь, что открываете файл с кодировкой UTF-8:
```python
with open('scraped_data/articles.json', 'r', encoding='utf-8') as f:
    data = json.load(f)
```
