# API Scripts

Коллекция скриптов для работы с API сервисами.

## Проверка AI API ключа

### validate-api-key.sh

Bash скрипт для проверки работоспособности OpenRouter API ключа.

#### Использование:

```bash
./apps/api/scripts/validate-api-key.sh <model> <api-key>
```

#### Пример:

```bash
./apps/api/scripts/validate-api-key.sh "google/gemini-2.0-flash-exp:free" "sk-or-v1-xxx"
```

#### Что проверяет:

- ✅ Валидность API ключа
- ✅ Доступность указанной модели
- ✅ Скорость ответа API
- ✅ Корректность ответа от AI

#### Возможные ошибки:

| Код | Описание | Решение |
|-----|----------|---------|
| 401 | Неверный API ключ | Проверьте ключ на https://openrouter.ai/settings/keys |
| 402 | Требуется оплата | Пополните баланс аккаунта OpenRouter |
| 429 | Превышен лимит запросов | Подождите или обновите тариф |
| 400 | Неверное имя модели | Проверьте имя модели на https://openrouter.ai/models |
| 500-503 | Ошибка сервиса | Проверьте статус на https://status.openrouter.ai |

### validate-api-key.js

Node.js версия скрипта валидации (использует встроенный fetch API).

#### Использование:

```bash
node apps/api/scripts/validate-api-key.js <model> <api-key>
```

### validate-api-key.ts

TypeScript версия скрипта (требует установки зависимостей).

#### Использование:

```bash
npx tsx apps/api/scripts/validate-api-key.ts <model> <api-key>
```

### test-qwen-model.ts

Полный набор тестов для проверки модели Qwen от Alibaba через OpenRouter.

#### Использование:

```bash
npx tsx apps/api/scripts/test-qwen-model.ts <api-key> [model]
```

#### Примеры:

```bash
# Тест с моделью по умолчанию (qwen/qwen3-4b:free)
npx tsx apps/api/scripts/test-qwen-model.ts "sk-or-v1-xxx"

# Тест с конкретной моделью
npx tsx apps/api/scripts/test-qwen-model.ts "sk-or-v1-xxx" "qwen/qwen3-32b-128k:free"
```

#### Что тестирует:

1. ✅ **Базовое соединение** - проверка доступности модели
2. ✅ **Генерация текста** - качество генерируемого контента
3. ✅ **Понимание языка** - корректность ответов на вопросы
4. ✅ **Многоязычность** - поддержка перевода и других языков
5. ✅ **Структурированный вывод** - генерация корректного JSON
6. ✅ **Обработка токенов** - правильное использование ограничений по длине

#### Результаты тестирования:

Скрипт выдает подробный отчет с:
- ✅/❌ статусом каждого теста
- ⏱️ временем отклика для каждого теста
- 📊 общей статистикой (процент успеха, среднее время отклика)

## Другие скрипты

### categorize-articles.ts

Скрипт для массовой категоризации статей с использованием AI.

```bash
npm run categorize:articles
```

## Примечания

- Скрипты валидации автоматически маскируют API ключи в выводе для безопасности
- Bash версия (`validate-api-key.sh`) рекомендуется для быстрой проверки
- Используйте флаг `-k` в curl для обхода проблем с SSL сертификатами в dev окружении
- Доступные модели Qwen можно найти на https://openrouter.ai/models?q=qwen
