# Aimak Akshamy - Сетевое издание

Полноценная система управления контентом для общественно-политического сетевого издания, построенная на современном стеке технологий.

## 🚀 Технологический стек

### Backend
- **NestJS** - прогрессивный Node.js фреймворк
- **Prisma** - современный ORM для TypeScript
- **PostgreSQL** - реляционная база данных
- **Redis** - кэширование и очереди
- **JWT** - аутентификация и авторизация
- **Swagger** - автоматическая документация API

### Frontend
- **Next.js 14** - React фреймворк с App Router
- **React 18** - библиотека для UI
- **TailwindCSS** - utility-first CSS фреймворк
- **React Query** - управление серверным состоянием
- **Zustand** - управление клиентским состоянием
- **Axios** - HTTP клиент

## 📋 Возможности

### Для всех пользователей
- ✅ Просмотр опубликованных статей
- ✅ Фильтрация по категориям
- ✅ Поиск по статьям
- ✅ Адаптивный дизайн
- ✅ Регистрация и авторизация

### Для редакторов (EDITOR)
- ✅ Создание и редактирование статей
- ✅ Публикация статей
- ✅ Добавление тегов
- ✅ Работа с изображениями
- ✅ Удаление своих статей

### Для администраторов (ADMIN)
- ✅ Все права редактора
- ✅ Управление категориями
- ✅ Управление тегами
- ✅ Удаление любых статей
- ✅ Просмотр статистики

## 🛠️ Установка и запуск

### Предварительные требования
- Node.js >= 18
- pnpm >= 8
- Docker и Docker Compose (для БД)

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd AIMAK
```

### 2. Установка зависимостей
```bash
pnpm install
```

### 3. Настройка переменных окружения

#### Быстрая настройка (Рекомендуется)

Используйте интерактивный скрипт:
```bash
./scripts/setup-env.sh
```

Скрипт поможет:
- ✅ Создать .env файлы из примеров
- ✅ Настроить API ключи для AI (Gemini/OpenRouter)
- ✅ Получить пошаговые инструкции

#### Ручная настройка

**Backend (.env в apps/api/)**
```bash
cp apps/api/.env.example apps/api/.env
```

Отредактируйте `apps/api/.env`:
```env
DATABASE_URL="postgresql://aimak_user:aimak_password_2024@localhost:5432/aimak_db?schema=public"
REDIS_HOST=localhost
REDIS_PORT=6379
PORT=4000
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this
JWT_REFRESH_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:3000

# AI Configuration (Рекомендуется для переводов и анализа)
GEMINI_API_KEY=your-gemini-api-key  # Получить: https://aistudio.google.com/app/apikey
GEMINI_MODEL=gemini-2.0-flash-exp
```

**Frontend (.env в apps/web/)**
```bash
cp apps/web/.env.example apps/web/.env
```

Отредактируйте `apps/web/.env`:
```env
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_APP_NAME=Aimak Akshamy
NEXT_PUBLIC_APP_DESCRIPTION=City Newspaper
```

**📖 Подробная документация:** См. [docs/deployment/ENVIRONMENT.md](./docs/deployment/ENVIRONMENT.md) для полного руководства по настройке переменных окружения

### 4. Запуск базы данных
```bash
docker-compose up -d postgres redis
```

### 5. Применение миграций
```bash
pnpm db:migrate
```

### 6. Запуск приложения

#### Development режим
```bash
pnpm dev
```

#### Production режим
```bash
pnpm build
pnpm start
```

## 🌐 Доступ к приложению

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:4000/api
- **API Documentation**: http://localhost:4000/api/docs
- **Prisma Studio**: `pnpm db:studio`

## 👥 Создание первого администратора

После первого запуска нужно создать администратора:

1. Зарегистрируйтесь через UI: http://localhost:3000/register
2. Откройте Prisma Studio: `pnpm db:studio`
3. Перейдите в таблицу `users`
4. Найдите своего пользователя и измените `role` на `ADMIN`
5. Обновите страницу в браузере

## Документация

Полная документация проекта находится в папке [docs/](./docs/README.md):

- **[Архитектура](./docs/ARCHITECTURE.md)** - общая архитектура системы
- **[API](./docs/api/README.md)** - REST API документация
- **[База данных](./docs/database/README.md)** - схема и модели Prisma
- **[Frontend](./docs/frontend/README.md)** - компоненты и хуки
- **[Развертывание](./docs/deployment/README.md)** - Docker, VPS, Plesk

### Руководства

- [Мультиязычность](./docs/guides/TRANSLATIONS.md) - работа с KZ/RU контентом
- [Социальные сети](./docs/guides/SOCIAL_MEDIA.md) - публикация в Telegram/Instagram
- [Рекламная система](./docs/guides/ADVERTISING.md) - настройка рекламы

## Структура проекта

```
AIMAK/
├── apps/
│   ├── api/                 # NestJS Backend
│   │   ├── src/
│   │   │   ├── articles/   # Статьи + AI анализ
│   │   │   ├── auth/       # Аутентификация (JWT)
│   │   │   ├── categories/ # Категории (иерархические)
│   │   │   ├── common/     # Prisma, Supabase, Storage
│   │   │   ├── magazine-issues/ # PDF журнал
│   │   │   ├── media/      # Загрузка файлов
│   │   │   ├── social-media/ # Telegram/Instagram
│   │   │   ├── tags/       # Теги + AI генерация
│   │   │   ├── translation/# AI перевод KZ↔RU
│   │   │   └── users/      # Пользователи
│   │   └── prisma/         # Схема БД и миграции
│   │
│   └── web/                # Next.js Frontend
│       └── src/
│           ├── app/        # App Router (страницы)
│           │   ├── [lang]/ # Мультиязычные маршруты
│           │   └── admin/  # Админ-панель
│           ├── components/ # React компоненты (36+)
│           ├── hooks/      # Custom хуки (10+)
│           ├── lib/        # Утилиты и API клиент
│           ├── store/      # Zustand stores
│           └── types/      # TypeScript типы
│
├── docs/                   # Документация проекта
├── scripts/                # Утилиты и скрипты
├── docker-compose.yml      # Docker конфигурация
└── pnpm-workspace.yaml     # Монорепо конфиг
```

## 🔐 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Статьи
- `GET /api/articles` - Список статей
- `GET /api/articles/:id` - Статья по ID
- `GET /api/articles/slug/:slug` - Статья по slug
- `POST /api/articles` - Создать статью (EDITOR/ADMIN)
- `PATCH /api/articles/:id` - Обновить статью (EDITOR/ADMIN)
- `DELETE /api/articles/:id` - Удалить статью (EDITOR/ADMIN)

### Категории
- `GET /api/categories` - Список категорий
- `GET /api/categories/:id` - Категория по ID
- `POST /api/categories` - Создать категорию (ADMIN)
- `PATCH /api/categories/:id` - Обновить категорию (ADMIN)
- `DELETE /api/categories/:id` - Удалить категорию (ADMIN)

### Теги
- `GET /api/tags` - Список тегов
- `GET /api/tags/:id` - Тег по ID
- `POST /api/tags` - Создать тег (EDITOR/ADMIN)
- `PATCH /api/tags/:id` - Обновить тег (ADMIN)
- `DELETE /api/tags/:id` - Удалить тег (ADMIN)

### Пользователи
- `GET /api/users` - Список пользователей (ADMIN)
- `GET /api/users/:id` - Пользователь по ID
- `PATCH /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя (ADMIN)

## 🧪 Тестирование

```bash
# Запуск тестов
pnpm test

# Запуск линтеров
pnpm lint
```

## 📝 Скрипты

```bash
# Development
pnpm dev              # Запуск в режиме разработки
pnpm db:migrate      # Применить миграции БД
pnpm db:studio       # Открыть Prisma Studio

# Production
pnpm build           # Сборка проекта
pnpm start           # Запуск production сборки

# Maintenance
pnpm lint            # Проверка кода
pnpm test            # Запуск тестов
```

## 🚀 Деплой

### Подготовка к деплою
1. Обновите переменные окружения для production
2. Измените `JWT_SECRET` на безопасный ключ
3. Настройте production базу данных
4. Настройте CDN для статических файлов

### Рекомендуемые платформы
- **Frontend**: Vercel, Netlify
- **Backend**: Railway, Render, DigitalOcean
- **Database**: Supabase, Neon, AWS RDS
- **Redis**: Upstash, Redis Cloud

## Реализованные возможности

- [x] Двуязычный контент (казахский/русский)
- [x] AI-перевод текстов (OpenRouter)
- [x] AI-категоризация и генерация тегов
- [x] Публикация в Telegram и Instagram
- [x] PDF-журнал с онлайн просмотром
- [x] Система рекламных блоков
- [x] Комментарии к статьям
- [x] Пагинация
- [x] Поиск по статьям
- [x] WYSIWYG редактор (TipTap)

## Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License.

## 👨‍💻 Автор

Проект разработан с использованием Claude AI.

## 🙏 Благодарности

- NestJS команда
- Next.js команда
- Prisma команда
- Все контрибьюторы open-source библиотек
